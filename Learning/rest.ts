function calculate1 (operator: string, ...numbers: number[]) {
    console.log (operator, numbers)
for (let i = 0; i < numbers.length; i++)
{let sum = sum + numbers [i]}
return sum
}

calculate1("/", 23, 50, 22)
